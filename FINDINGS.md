# FINDINGS — defect ledger

One row per finding: date, where, severity, what, how found, suggested fix, status.
Found by a six-way parallel code review (2026-08-22) over both platforms; the two
tower HIGHs and all runner MEDs were reproduced empirically, not just read.

| date | where | sev | what | how found | suggested fix | status |
|---|---|---|---|---|---|---|
| 2026-08-22 | `src/brainrot/scenes/spiralplan.py:2884` | HIGH | `spawn()` sets `self.u = self.u_of(blk)`; `unwrap` resolves *at or below* the landing's height, so a legal landing below the floor at its bearing (authored exit treads, descending crossings) snaps `u` back a full revolution. Tower run 1: 696 full-revolution regressions in 4,000 landings, tops out at level 77 vs 132 clean. Seeds 7 and 11 under the renderer's advance discipline enter a *permanent* 3-level loop — the strip stops climbing for good. Generated spiral clean (0 in 18,000). Almost certainly the root of the "give-back" outstanding item and the owner's THE VAULT loop report. | review + instrumented repro | advance `u` via `_u_at` (bearing-relative, hint = current u) instead of height-resolved `u_of`; never let a commit move `u` backward by ~2π | open |
| 2026-08-22 | `src/brainrot/scenes/spiralplan.py:2877-2883` | HIGH | A landing placed by `_last_resort`/`_climb_on`/`_grab_the_wall` is committed with the **failed** node's metadata: `_commit(prev, node, got)` reads style/label/origin/lift/moat/shell/`cross`/`from_level` off the abandoned node. Measured: 63 recoveries over 10 runs, zero committed as `recover`; 32 stamped as crossings with false `to_level`. `test_tower.py:454`'s recover-share bound is vacuous; tower_probe's "reliability chain: 0" is unverifiable; endurance probe's crossing counter picks up false crossings; moats/shells built around recovery landings the node never described. | review + instrumented repro | have `_last_resort` return (got, its own node) and commit that; keep `label="recover"`/`origin="recover"` on the block | open |
| 2026-08-22 | `src/brainrot/ipc.py:67` + `:141` | HIGH | `Event.parse` guards `json.loads` but not the `pids` loop: `{"pids": 5}` raises `TypeError`, and `Event.parse(payload)` in `EventListener._run` sits outside the per-event try — one malformed datagram permanently kills the listener thread; daemon goes silently deaf (no shows, no hides) until restart. | review + repro (`Event.parse(b'{"kind":"Stop","pids":5}')`) | wrap the whole of `parse` body in the existing try, or wrap the `parse` call at `:141` in the same except as the handler | open |
| 2026-08-22 | `src/brainrot/state.py:194,209` + `engine/loop.py:174` | HIGH | `ThinkingState.tick()` runs on both the IPC thread (via `handle()`) and the render thread with no lock; it iterates `_thinking` while the other thread `add`/`discard`s → `RuntimeError: set changed size during iteration`. On the render thread the call is outside any except, so it exits the daemon. Secondary: interleaved `tick()`s can trip the `assert self._busy_since is not None` at `state.py:244`. Comment at `loop.py:84-86` ("render thread only reads derived values") is wrong — `tick()` mutates. Flagged independently by three reviewers. | review (3× independent) | one small `threading.Lock` around `handle()`/`tick()`, or stop calling `tick()` from `handle()` and snapshot `_thinking` before iterating | open |
| 2026-08-22 | `tests/test_overlay_win32.py:238` (+~14 sites) | HIGH | Win32 suite still calls `OverlayWindow._hwnd()`/`._host_hwnd`, renamed to `._win`/`._host` in the Linux-port commit (`810a733`); fixture also builds `OverlayWindow()` without `create()`, so `_native` is None and `attach_host` returns early. Every `window`-fixture test fails on Windows with `AttributeError`; suite skips on Linux, so all Windows regressions are currently invisible. | review + git history | mechanical rename plus fixture update to call `create()`; run on the Windows box | open |
| 2026-08-22 | `src/brainrot/scenes/runner.py:976` vs `:1212-1230` | MED | `_rewind` truncates entities/spans/acts but not the `spawned` counters, and several set-pieces add entities before declining (`_seg_yard`, `_seg_express`, `_seg_hurdles`, roofrun belt-and-braces). Measured: 3,822 rewound coins still counted over 8×300 rows; on one run ~50% of counted coins never existed. `runner_probe`'s obstacle mix / rates / coin figures — the acceptance numbers in CLAUDE.md — read a ledger that counts ghosts. Gameplay unaffected. | review + instrumented repro | snapshot `spawned` in `_mark`, restore in `_rewind` | open |
| 2026-08-22 | `src/brainrot/scenes/runner.py:2628` | MED | `_crash_step` does `self.elapsed += dt` but every driver (loop.py:207, cli.py:131, both probes, tests) already advances `elapsed` after `update()` — clock double-advances through a wreck (measured +3.85 s over a 1.93 s hold). Injects ~1.9 s phantom idle per wreck into pacing/dead-air stats (plausible contributor to outstanding 0000b) and can miss `speed_at` marks. | review + repro | delete the `self.elapsed += dt` line | open |
| 2026-08-22 | `src/brainrot/scenes/runner.py:2700-2703,2903-2907` + `runnerplan.py:466-469` | MED | Roof-crossing gate ("far lane is a roof for the whole crossing") samples the far lane at `speed × lane_time` spacing — 4.08 m at 24 m/s, 5.10 m at 30 — against `ROOF_GAP` of 3.4-4.0 m, so a whole convoy gap fits between samples; `verify` uses the same samples so the self-check cannot catch it. Candidate mechanism for the un-attributed air-contact half of outstanding 0000a. | review + arithmetic | make the check gap-aware (walk the far chain's spans) rather than adding samples | open |
| 2026-08-22 | `src/brainrot/overlay/win32.py:412,430` | MED | `SetForegroundWindow` is the one call with no argtypes/restype (64-bit handle truncation class the file promises to exclude) and its BOOL result is ignored; Windows routinely refuses it to a background process, at which point takeover has already stripped `WS_EX_TRANSPARENT|WS_EX_NOACTIVATE` — strip stops being click-through, eats clicks, focus never arrives, until chord toggles again. `input._grab` only backs out on exception. | review | declare types; on FALSE restore the ex-style and abort the takeover | open |
| 2026-08-22 | `engine/loop.py:105` + `overlay/win32.py:197-201` | MED | `attach_host` runs on the IPC thread while `detach_host`/`set_visible` run on the render thread, unsynchronised; interleaving can leave the daemon idle-and-owned — exactly the state detach-on-idle exists to prevent (host closes → `DestroyWindow` takes the hidden overlay window). Flagged independently by two reviewers. | review | queue host attachment onto the render thread (flag consumed in the frame loop) instead of attaching from `_on_event` | open |
| 2026-08-22 | `overlay/shell.py:176-180` | MED | `host_for`'s `by_pid` dict keeps an arbitrary (last-in-actor-order) window when one pid owns several — gnome-terminal-server / VS Code case: strip can dock beside the wrong window and hide while the real Claude Code window is in front. | review | prefer the focused window among same-pid candidates | open |
| 2026-08-22 | `engine/loop.py:203-223` | MED | Scene-draw error recovery catches after `window.begin()` but never calls `end()`: rlgl projection push leaks per attempt (stack depth 32), no present/input poll that frame; on the scaled path `BeginTextureMode` stays open. A deterministic draw bug degrades the daemon's GL state progressively. | review | `try: begin(); draw() finally: end()` in the recovery path | open |
| 2026-08-22 | `src/brainrot/config.py:129-136,160-170` | MED | toml values applied untyped; `_validate` then raises out of `Config.load`, so one wrong-typed line (`width = "abc"`) crashes every command **including `brainrot doctor`** — the tool meant to diagnose it. Unvalidated fields leak through (`port = "47821"` stays str → `TypeError` at bind, bypassing the friendly AlreadyRunning path; `scenes = "runner"` iterated char-wise, silently all scenes). Contradicts the module's own stated policy for env overrides. | review + repro | coerce/validate per-field with a warning-and-default policy matching env overrides | open |
| 2026-08-22 | `src/brainrot/hookinstall.py:184-191` | MED | `install()` de-duplicates only the events being registered, so re-installing without `--with-tool-events` leaves the PreToolUse/PostToolUse shims in settings.json forever (shim spawn on every tool call) until full uninstall. | review + repro | sweep all events for `_is_ours` entries before re-adding, as `uninstall()` does | open |
| 2026-08-22 | `engine/loop.py:180-181` | MED | `_begin_scene()` sits outside the per-frame try, so a scene *construction* error (e.g. `force_scene` typo → `KeyError`, or a seed-specific build bug) exits the daemon at first show, defeating the "a bug in one world costs that world, not the daemon" policy at `:213`. | review | wrap `_begin_scene` in the same forgive-and-reroll handler | open |
| 2026-08-22 | `src/brainrot/scenes/parkourkit.py:555-561` | LOW | `wrap_angle` is O(angle) (repeated subtraction) and `_u_at` feeds it unbounded `th - u_ref` on the per-candidate ranking path — ~300 loop iterations per call after a 4-hour run; monotonic slowdown with altitude. | review | `math.fmod` | open |
| 2026-08-22 | `spiralplan.py:4242-4298` + `handplan.py:272-297` | LOW | Long-run memory growth: `struct`, `carved`, terrain cells in `solid`, and `noclimb` (~27 cells/landing) are never pruned — order 50-100 MB over a 4-h endurance run. Correctness unaffected. | review + measure | prune behind the body the way per-block cells already are | open |
| 2026-08-22 | `engine/voxel.py:386-401,421-424` + `assets/__init__.py:103-107,160-172` | LOW | Two C-heap leaks per scene build/unload cycle: `block_model()` per-model arrays never freed in `clear()` (can't `UnloadModel` — shared mesh — but nothing frees the rest), and `unload_all()` never calls `UnloadModelAnimations` (largest allocation an asset owns). Tens of KB per show; unbounded in a long-lived daemon. | review | explicit `MemFree` of model arrays in `clear()`; store anim count and `UnloadModelAnimations` in `unload_all` | open |
| 2026-08-22 | `engine/window.py:399` | LOW | `compute_placement(cfg, self._ratio)[:2]` — `Placement` is a frozen dataclass, not subscriptable; the no-native-backend fallback branch crashes instead of degrading. | review | use `.x`/`.y` | open |
| 2026-08-22 | `engine/window.py:353-373` + `overlay/win32.py` | LOW | On Windows the overlay is born visible+topmost and calibration marker frames draw before styles apply — one topmost flash per daemon start, against the never-in-front rule. Linux backend sets `CREATE_HIDDEN`; win32 does not. | review | `CREATE_HIDDEN = True` in win32 + `set_window_shown` on first show | open |
| 2026-08-22 | `src/brainrot/extinstall.py:110-112,141-145` | LOW | `_mark_enabled` fails on `@as []` (machines with zero extensions enabled — exactly the fresh-GNOME case); custom port is written only when != default, so reverting config to default never repairs the extension's stale port. | review | strip `@as ` prefix; write port unconditionally | open |
| 2026-08-22 | `src/brainrot/extension/.../extension.js:75-97,213` | LOW | `disable()` during an active takeover never sends the off-packet nor restores focus to `_takeoverReturnTo` (lock screen disables session-mode "user" extensions, so this fires in practice); heartbeat at `PRIORITY_DEFAULT_IDLE` vs the daemon's 3 s staleness cutoff can flap degraded mode under compositor load. | review | give-back half of `_toggleTakeover` in `disable()`; heartbeat at `PRIORITY_DEFAULT` | open |
| 2026-08-22 | `engine/window.py:448-463` | LOW | If the X11 window scan ever yields `_win == 0`, every native call returns silently, the raylib fallback is never taken, `_visible` reads True — strip permanently invisible with no symptom. | review | explicit fallback to `super().set_visible()` (or a printed warning) when `_win` is 0 | open |
| 2026-08-22 | `src/brainrot/doctor.py:334-339` + `hookinstall.py:151-155` | LOW | Malformed hand-edited settings.json entries (non-dict in a hooks list) crash `brainrot install` and the whole doctor run with `AttributeError` instead of a FAIL row. | review | isinstance-guard entries; malformed → FAIL row | open |
| 2026-08-22 | `state.py:165-167` + `engine/loop.py:99,242` | LOW | `_hosts` pruned only on SessionEnd (Stop/expiry leak entries for daemon lifetime); `_unresolved` never pruned and `_retry_hosts` re-resolves dead sessions every frame. Bytes trivial; per-frame resolve calls creep. | review | prune both wherever a session is retired | open |
