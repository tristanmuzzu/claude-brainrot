pygame-ce>=2.5
# Bakes textures, clouds and sphere sprites at scene construction.
numpy>=1.24
# Windows only -- enables the click-through, always-on-top overlay.
pywin32>=306; sys_platform == "win32"
